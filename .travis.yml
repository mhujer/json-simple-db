language: php

php:
  - 5.6
  - 7
  - hhvm

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - composer update

script:
  - mkdir -p build/logs
  - ./vendor/bin/phpunit

after_script:
  - php vendor/bin/coveralls -v
